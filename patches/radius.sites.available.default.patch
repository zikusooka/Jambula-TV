--- /etc/raddb/sites-available/default.orig	2015-05-25 16:50:42.461037558 +0300
+++ /etc/raddb/sites-available/default	2015-05-25 17:25:39.235055967 +0300
@@ -259,7 +259,7 @@
 	#
 	#  See policy.d/filter for the definition of the filter_username policy.
 	#
-	filter_username
+	#filter_username # Jambula: This causes problems with logins
 
 	#
 	#  The preprocess module takes care of sanitizing some bizarre
@@ -285,7 +285,7 @@
 	#
 	#  If you want to have a log of authentication requests,
 	#  un-comment the following line.
-#	auth_log
+	auth_log
 
 	#
 	#  The chap module will set 'Auth-Type := CHAP' if we are
@@ -357,7 +357,7 @@
 	#  to read /etc/passwd or /etc/shadow directly, see the
 	#  passwd module in radiusd.conf.
 	#
-#	unix
+	unix
 
 	#
 	#  Read the 'users' file
@@ -368,9 +368,12 @@
 	#  is meant to mirror the "users" file.
 	#
 	#  See "Authorization Queries" in sql.conf
-	-sql
+	sql
+	
+	# Jambula Labs - SQL counters
+	chillispot_max_bytes
+	noresetcounter
 
-	#
 	#  If you are using /etc/smbpasswd, and are also doing
 	#  mschap authentication, the un-comment this line, and
 	#  configure the 'smbpasswd' module.
@@ -382,7 +385,7 @@
 
 	#
 	#  Enforce daily limits on time spent logged in.
-#	daily
+	#daily
 
 	#
 	expiration
@@ -576,7 +579,7 @@
 	#  Note that accounting requests which are proxied
 	#  are also logged in the detail file.
 	detail
-#	daily
+	#daily
 
 	#  Update the wtmp file
 	#
@@ -588,7 +591,7 @@
 	#
 	#  Due to packet losses in the network, the data here
 	#  may be incorrect.  There is little we can do about it.
-#	radutmp
+	radutmp
 #	sradutmp
 
 	#  Return an address to the IP Pool when we see a stop record.
@@ -598,7 +601,7 @@
 	#  Log traffic to an SQL database.
 	#
 	#  See "Accounting queries" in sql.conf
-	-sql
+	sql
 
 	#
 	#  If you receive stop packets with zero session length,
@@ -642,11 +645,11 @@
 #  or rlm_sql module can handle this.
 #  The rlm_sql module is *much* faster
 session {
-#	radutmp
+	radutmp
 
 	#
 	#  See "Simultaneous Use Checking Queries" in sql.conf
-#	sql
+	sql
 }
 
 
@@ -690,13 +693,13 @@
 	#  If you want to have a log of authentication replies,
 	#  un-comment the following line, and enable the
 	#  'detail reply_log' module.
-#	reply_log
+	reply_log
 
 	#
 	#  After authenticating the user, do another SQL query.
 	#
 	#  See "Authentication Logging Queries" in sql.conf
-	-sql
+	sql
 
 	#
 	#  Instead of sending the query to the SQL server,
