# Laptop battery is discharging
alias: "System Battery is discharging"
initial_state: True


trigger:

  - platform: state
    entity_id: sensor.battery_laptop
    to: 'Discharging'


#condition:
  #condition: and
  #conditions:

    # Internet state
    #- condition: template
    #  value_template: "{{ is_state('binary_sensor.internet', 'on') }}"


action:

  # Run script
  # ----------
  - service: script.turn_on
    entity_id: script.laptop_battery_discharging
