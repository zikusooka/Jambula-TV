alias: Bedtime for everyone
initial_state: True

trigger:

  - platform: state
    entity_id: input_boolean.bedtime_mode_all
    to: 'on'


condition:
  condition: and
  conditions:

    # After sunset
    #- condition: sun
     # before: sunrise
      

action:

  #  SCRIPT
  # -------
  - service: script.turn_on
    data:
      entity_id:
        - script.bedtime_all
