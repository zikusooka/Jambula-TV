alias: Wakeup for Maid
initial_state: True

trigger:

  - platform: time
    at: '07:30:00'


condition:
  condition: and
  conditions:

    # After sunset
    #- condition: sun
      #after: sunrise

action:

  #  SCRIPT
  # -------
  - service: script.turn_on
    data:
      entity_id:
        - script.wakeup_maid
