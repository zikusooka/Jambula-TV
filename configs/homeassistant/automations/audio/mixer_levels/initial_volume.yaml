# Reset audio volume to defined initial levels 
alias: "Set initial audio volume at start time"
initial_state: True

trigger:
  - platform: homeassistant
    event: start


action:
  - service: shell_command.speaker_volume_input
    data_template:
      volume_level: !secret mixer_start_up_volume_level
