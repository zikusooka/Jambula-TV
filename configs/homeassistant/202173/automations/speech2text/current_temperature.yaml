# Speech-to-Text: Read the current temperature (Outside)
alias: "Read the current temperature outside"
initial_state: True


trigger:
  - platform: mqtt
    topic: !secret mqtt_topic_stt_read_temperature_outside


condition:
  condition: and
  conditions:


action:

  #  SCRIPT
  # -------
  - service: script.turn_on
    data:
      entity_id:
        - script.temperature_current_outside
