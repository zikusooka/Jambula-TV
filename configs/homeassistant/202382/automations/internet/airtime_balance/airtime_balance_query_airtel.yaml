# Data - Query Airtime Balance (Airtel)
alias: "Query Airtime Balance (Airtel)"
initial_state: True

trigger:
  - platform: time_pattern
    hours: '/3'


condition:
  condition: and
  conditions:



action:

  # Run script
  # ----------
  - service: script.turn_on
    entity_id: script.airtime_query_balance_airtel
