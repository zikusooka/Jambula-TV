type: entity-button
entity: script.garbage_collected
tap_action:
  action: call-service
  confirmation:
    text: Garbage collected?
  service: script.turn_on
  service_data:
    entity_id: script.garbage_collected
name: 'Garbage collected'
show_name: true
show_state: false
icon: 'mdi:delete-empty'
