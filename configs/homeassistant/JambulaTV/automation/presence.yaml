trigger:
  platform: state
  entity_id: device_tracker.zik
  from: 'Away'
  to: 'Home'
action:
  service: shell_command.send_telegram_text
  data_template:
    message: '{{"Presence Alert *Zik* is *Home*"}}'
    phone: ''
