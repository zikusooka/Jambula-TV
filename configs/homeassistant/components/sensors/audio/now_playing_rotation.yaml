platform: command_line
name: "Now playing rotation"
command: /bin/bash -c "if [[ -s $(/usr/bin/jambulatv-homeassistant-secrets radio_rotation_playing_file) ]]; then /usr/bin/cat $(/usr/bin/jambulatv-homeassistant-secrets radio_rotation_playing_file); else echo ''; fi"
scan_interval: 10
