# Office - Call the maid for immediate assistance
# -----------------------------------------------
call_maid_office:
  sequence:

 
    # Wait until voice alerts mixer status is 'off'
    # ---------------------------------------------
    - service: script.wait_for_availability_of_mixer_voice_alerts


    # Set status of voice alerts mixer to 'on'
    # ----------------------------------------
    - service: homeassistant.turn_on
      data:
        entity_id: input_boolean.voice_alerts_mixer_status


    # Voice Alert
    # -----------
    # a) Run script: If music is playing, pause it
    - service: script.music_pause_if_playing

    # b) Play initial sound effect using house speaker 
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Dissonant-bells-alert-tone'
        audio_gain: !secret voice_alert_audio_gain_medium

    # c) Play sound effect using house speaker
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Dissonant-bells-alert-tone'
        audio_gain: !secret voice_alert_audio_gain_medium

    # d) Say maid is needed immediately
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'immediate_assistance_is_needed_in_the_office'
        audio_gain: !secret voice_alert_audio_gain_medium

    # f) Run script: If music is paused, unpause player
    - service: script.music_unpause_if_paused


    # Set status of voice alerts mixer to 'off'
    # ----------------------------------------
    - service: homeassistant.turn_off
      data:
        entity_id: input_boolean.voice_alerts_mixer_status



# Verandah - Call the maid for immediate assistance
# -------------------------------------------------
call_maid_verandah:
  sequence:


    # Wait until voice alerts mixer status is 'off'
    # ---------------------------------------------
    - service: script.wait_for_availability_of_mixer_voice_alerts


    # Set status of voice alerts mixer to 'on'
    # ----------------------------------------
    - service: homeassistant.turn_on
      data:
        entity_id: input_boolean.voice_alerts_mixer_status

    # Voice Alert
    # -----------
    # a) Run script: If music is playing, pause it
    - service: script.music_pause_if_playing

    # b) Play initial sound effect using house speaker 
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Dissonant-bells-alert-tone'
        audio_gain: !secret voice_alert_audio_gain_medium

    # c) Play sound effect using house speaker
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Dissonant-bells-alert-tone'
        audio_gain: !secret voice_alert_audio_gain_medium

    # d) Say maid is needed immediately
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'immediate_assistance_is_needed_at_the_verandah'
        audio_gain: !secret voice_alert_audio_gain_medium

    # e) Run script: If music is paused, unpause player
    - service: script.music_unpause_if_paused


    # Set status of voice alerts mixer to 'off'
    # ----------------------------------------
    - service: homeassistant.turn_off
      data:
        entity_id: input_boolean.voice_alerts_mixer_status



# Bedroom - Call the maid for immediate assistance
# -------------------------------------------------
call_maid_bedroom:
  sequence:


    # Wait until voice alerts mixer status is 'off'
    # ---------------------------------------------
    - service: script.wait_for_availability_of_mixer_voice_alerts


    # Set status of voice alerts mixer to 'on'
    # ----------------------------------------
    - service: homeassistant.turn_on
      data:
        entity_id: input_boolean.voice_alerts_mixer_status


    # Voice Alert
    # -----------
    # a) Run script: If music is playing, pause it
    - service: script.music_pause_if_playing

    # b) Play initial sound effect using house speaker 
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Dissonant-bells-alert-tone'
        audio_gain: !secret voice_alert_audio_gain_medium

    # c) Play sound effect using house speaker
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Dissonant-bells-alert-tone'
        audio_gain: !secret voice_alert_audio_gain_medium

    # d) Say maid is needed immediately
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'immediate_assistance_is_needed_in_the_bedroom'
        audio_gain: !secret voice_alert_audio_gain_medium

    # e) Run script: If music is paused, unpause player
    - service: script.music_unpause_if_paused


    # Set status of voice alerts mixer to 'off'
    # ----------------------------------------
    - service: homeassistant.turn_off
      data:
        entity_id: input_boolean.voice_alerts_mixer_status



# Porch - Call the maid for immediate assistance
# ----------------------------------------------
call_maid_porch:
  sequence:


    # Wait until voice alerts mixer status is 'off'
    # ---------------------------------------------
    - service: script.wait_for_availability_of_mixer_voice_alerts


    # Set status of voice alerts mixer to 'on'
    # ----------------------------------------
    - service: homeassistant.turn_on
      data:
        entity_id: input_boolean.voice_alerts_mixer_status


    # Voice Alert
    # -----------
    # a) Run script: If music is playing, pause it
    - service: script.music_pause_if_playing

    # b) Play initial sound effect using house speaker 
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Dissonant-bells-alert-tone'
        audio_gain: !secret voice_alert_audio_gain_medium

    # c) Play sound effect using house speaker
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Dissonant-bells-alert-tone'
        audio_gain: !secret voice_alert_audio_gain_medium

    # d) Say maid is needed immediately
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'immediate_assistance_is_needed_at_the_porch'
        audio_gain: !secret voice_alert_audio_gain_medium

    # e) Run script: If music is paused, unpause player
    - service: script.music_unpause_if_paused


    # Set status of voice alerts mixer to 'off'
    # ----------------------------------------
    - service: homeassistant.turn_off
      data:
        entity_id: input_boolean.voice_alerts_mixer_status
