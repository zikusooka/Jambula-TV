its_time_for_lunch:
  sequence:


    # Task - Announce that its time for lunch
    # ----------------------------------------
    # a) Run script: If music is playing, pause it
    - service: script.music_pause_if_playing

    # b) Its time for lunch sound effect using house speakers
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Bell-magic-transition-sound-effect'
        audio_gain: !secret voice_alert_audio_gain_medium

    # c) Announce its time time for lunch using house speakers
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'its_time_4_lunch'
        audio_gain: !secret voice_alert_audio_gain_high

    # d) Run script: If music is paused, unpause player
    - service: script.music_unpause_if_paused
