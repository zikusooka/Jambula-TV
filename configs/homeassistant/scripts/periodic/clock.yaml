say_time_at_top_off_hour:
  sequence:


    # Task - Announce the current time at the top of the hour
    # -------------------------------------------------------
    #
    # a) Run script: If music is playing, pause it
    - service: script.music_pause_if_playing

    # b) Big Ben chime alert using house speakers
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: '22'
        audio_gain: !secret voice_alert_audio_gain_low

    # c) Wait for bell to finish ringing
    - delay: '00:00:08'

    # d) Speak the current hour using house speakers
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: '41'
        audio_gain: !secret voice_alert_audio_gain_medium

    # e) Run script: If music is paused, unpause player
    - service: script.music_unpause_if_paused
