water_flow_on_alert:
  sequence:

  # 1. Alert using house speakers
  # -----------------------------

    # a) Run script: If music is playing, pause it
    - service: script.music_pause_if_playing

    # b) Water supply is on sound effect using house speakers
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'Sound-of-water-flowing'
        audio_gain: !secret voice_alert_audio_gain_medium

    # c) Announce water supply is on using house speakers
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'good_news'
        audio_gain: !secret voice_alert_audio_gain_medium
    - service: shell_command.voice_alerts_and_sound_effects
      data_template:
        sound_alert: 'water_supply_is_back'
        audio_gain: !secret voice_alert_audio_gain_medium

    # d) Run script: If music is paused, unpause player
    - service: script.music_unpause_if_paused
