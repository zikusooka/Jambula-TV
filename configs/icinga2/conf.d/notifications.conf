apply Notification "OSD" to Service {

       import "on-screen-display-notification"

       user_groups = [ "icingaadmins" ]

       assign where match("*on screen display*", service.notes) && host.name == "JambulaTV" 
}
