    server {
        listen 8522;

        server_name  jambulatv_rtgui;
        root   html/rtgui;
        index  index.html index.htm index.php;

        location / {
        }

        location ~ \.php$ {
            include  includes/php_params;
        }
 
    	location ~* \.(cgi|chi)$ {
            include  includes/cgi_params;
    	}  

    	location ~ ^/RPC2$ {
	    include scgi_params;
            scgi_pass unix:/JambulaTV/rpc.socket;
    	}

    }
